import React from "react";
import { connect } from "react-redux";
import { addValue, subtractValue } from "../../store/actions";

const AddSubtractCounter = () => {
    return <div></div>;
};

export default connect(null)(AddSubtractCounter);
